declare module '@logpose/eslint-config/node.eslint.config.js' {
  import type { ConfigArray } from 'typescript-eslint';

  const config: ConfigArray;
  export default config;
}

declare module '@logpose/eslint-config/nest.eslint.config.js' {
  import type { ConfigArray } from 'typescript-eslint';

  const config: ConfigArray;
  export default config;
}

declare module '@logpose/eslint-config/react.eslint.config.js' {
  import type { ConfigArray } from 'typescript-eslint';

  const config: ConfigArray;
  export default config;
}
